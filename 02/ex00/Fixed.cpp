#include "Fixed.hpp"

Fixed::Fixed()
{
}

Fixed::Fixed(Fixed const& copy)
{
	//assigner les attributs
	return (*this);
}

Fixed::~Fixed()
{
}

Fixed	Fixed::operator=(Fixed const& ref)
{
	return (*this);
}
