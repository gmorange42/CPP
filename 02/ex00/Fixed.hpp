#ifndef FIXED_HPP
#define FIXED_HPP

#include <iostream>
#include <string>

class	Fixed
{
	public:
		Fixed();
		Fixed(Fixed& copy);
		~Fixed();
		Fidex&	operator=(Fixed const& ref)
	private:
};

#endif
